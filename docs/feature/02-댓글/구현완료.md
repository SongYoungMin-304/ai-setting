# 댓글/대댓글 기능 - 구현 완료 문서

**작성일**: 2026-04-20
**상태**: ✅ 완료
**버전**: Phase 1

---

## 📋 개요

게시글 상세 페이지에 **재귀 구조의 중첩 댓글/대댓글 시스템**을 구현했습니다.

**핵심 특징**:
- ✅ 무한 깊이 지원 (재귀 구조)
- ✅ 실시간 UI 업데이트 (모킹 데이터)
- ✅ 권한 기반 삭제 (본인만 가능)
- ✅ 타입 안전 (TypeScript)
- ✅ Tailwind CSS 스타일

---

## 📁 구현 파일

### 신규 파일
```
src/components/
├── CommentForm.tsx       (재사용 가능한 폼)
├── CommentItem.tsx       (개별 댓글 + 재귀)
├── CommentList.tsx       (최상위 댓글 목록)
└── CommentSection.tsx    (상태 관리)

src/services/
└── commentService.ts     (API 메서드 정의)
```

### 수정 파일
```
src/types/index.ts              (Comment, CommentsResponse 추가)
src/pages/PostDetailPage.tsx    (CommentSection 통합)
```

---

## 🎯 구현된 기능

### 1. 댓글 작성 ✅
```typescript
// CommentForm.tsx에서 처리
- 유효성 검사: 빈 입력 거부, 500자 제한
- 에러 메시지 표시
- 로딩 상태 관리
```

### 2. 댓글 목록 조회 ✅
```typescript
// CommentSection에서 모킹 데이터 제공
- 4개의 샘플 댓글
- 3단계까지의 중첩 구조
- 사용자 정보 포함
```

### 3. 대댓글(답글) 작성 ✅
```typescript
// handleReplySubmit에서 재귀 함수 사용
const addReplyRecursive = (arr, parentId, reply) => {
  return arr.map(c => ({
    ...c,
    replies: c.id === parentId 
      ? [...c.replies, reply]
      : addReplyRecursive(c.replies, parentId, reply)
  }));
};
```

### 4. 댓글 삭제 ✅
```typescript
// handleDelete에서 재귀 함수 사용
const deleteCommentRecursive = (arr, id) => {
  return arr
    .filter(c => c.id !== id)
    .map(c => ({
      ...c,
      replies: deleteCommentRecursive(c.replies, id)
    }));
};
```

### 5. 권한 관리 ✅
```typescript
// 본인 댓글에만 삭제 버튼 표시
const isAuthor = user?.id === comment.author.id;
{isAuthor && <button onClick={() => onDelete(comment.id)}>삭제</button>}
```

### 6. 재귀 구조 ✅
```typescript
// CommentItem이 자신을 재귀 호출
{comment.replies?.map(reply => (
  <CommentItem key={reply.id} comment={reply} {...props} />
))}
```

---

## 🏗️ 컴포넌트 계층

```
CommentSection (상태 관리)
  ├─ CommentForm (댓글 작성)
  └─ CommentList (최상위 댓글)
      └─ CommentItem (개별 댓글)
          ├─ CommentForm (답글 작성)
          └─ CommentItem 재귀 (대댓글...)
              ├─ CommentForm (깊은 답글)
              └─ CommentItem 재귀 (더 깊은 대댓글...)
```

---

## 📊 데이터 모델

### Comment 타입
```typescript
interface Comment {
  id: number;
  postId: number;
  content: string;
  author: {
    id: number;
    username: string;
    profileImage?: string;
  };
  createdAt: string;
  updatedAt: string;
  replies: Comment[];         // 재귀 배열
  parentCommentId?: number;   // 부모 댓글 ID
}
```

### 샘플 구조
```json
{
  "id": 1,
  "content": "좋은 글입니다!",
  "author": { "id": 2, "username": "user123" },
  "replies": [
    {
      "id": 2,
      "content": "감사합니다!",
      "author": { "id": 1, "username": "작성자" },
      "parentCommentId": 1,
      "replies": [
        {
          "id": 3,
          "content": "기다리고 있을게요!",
          "parentCommentId": 2,
          "replies": []
        }
      ]
    }
  ]
}
```

---

## 💾 상태 관리

### CommentSection의 State
```typescript
const [comments, setComments] = useState<Comment[]>(MOCK_COMMENTS);
const [loading, setLoading] = useState(false);
const [error, setError] = useState<string | null>(null);
const [showReplyForm, setShowReplyForm] = useState<{
  [commentId: number]: boolean;
}>({});
```

### 상태 업데이트 패턴
```typescript
// 댓글 추가: 배열에 새 항목 추가
setComments([...comments, newComment]);

// 대댓글 추가: 재귀 함수로 깊은 위치 업데이트
setComments(addReplyRecursive(comments, parentId, newReply));

// 삭제: 재귀 함수로 깊은 위치 삭제
setComments(deleteCommentRecursive(comments, commentId));

// 폼 토글: 객체 키 관리
setShowReplyForm({ ...showReplyForm, [commentId]: !showReplyForm[commentId] });
```

---

## 🎨 UI 스타일

### CommentForm
- **댓글**: `mb-6` (기본)
- **답글**: `ml-12 mt-2 mb-4` (들여쓰기)
- **버튼**: 파란색(작성), 회색(취소)
- **에러**: 빨간색 텍스트

### CommentItem
- **헤더**: 프로필 + 유저명 + 시간 + 삭제 버튼
- **본문**: 회색 텍스트 (`text-gray-800`)
- **액션**: "답글" 링크 스타일
- **들여쓰기**: `ml-6 border-l border-gray-300 pl-4`

### 색상 팔레트
```
- Primary: Blue 500 (작성 버튼)
- Danger: Red 500 (삭제)
- Border: Gray 300
- Text: Gray 800
- Secondary: Gray 500 (시간, 보조 정보)
```

---

## 🔧 API 서비스

### commentService.ts
```typescript
export const commentService = {
  getComments: (postId) => api.get(`/posts/${postId}/comments`),
  createComment: (postId, content) => api.post(`/posts/${postId}/comments`, { content }),
  createReply: (parentCommentId, content) => api.post(`/comments/${parentCommentId}/replies`, { content }),
  deleteComment: (commentId) => api.delete(`/comments/${commentId}`),
};
```

**현재 상태**: 메서드 정의만 완료 (API 호출 미적용)
**다음 단계**: 백엔드 API 준비 후 CommentSection에서 활성화

---

## 🧪 테스트 결과

### 통과한 테스트
- [x] UI 렌더링: 모든 컴포넌트 정상 표시
- [x] 모킹 데이터: 4개 댓글, 3단계 중첩
- [x] 대댓글 작성: 무한 깊이 지원
- [x] 삭제 기능: 재귀 삭제 정상
- [x] 권한 관리: 본인만 삭제 가능
- [x] 재귀 구조: CommentItem 재귀 호출 정상
- [x] 유효성 검사: 빈 입력, 길이 제한
- [x] 인증 처리: 로그인 상태 반영
- [x] 타입 안전성: TypeScript 에러 없음
- [x] 상태 관리: Props 전파, 불변성 유지

### QA 결과
**종합 판정**: ✅ PASS
**상세 보고서**: QA보고서.md 참고

---

## 📝 코드 예제

### 재귀 구조 - CommentItem.tsx
```typescript
export default function CommentItem({
  comment,
  onDelete,
  onReplySubmit,
  showReplyForm,
  onToggleReplyForm,
}: CommentItemProps) {
  return (
    <div>
      {/* 헤더, 본문, 액션 버튼 */}
      
      {/* 답글 폼 */}
      {showReplyForm[comment.id] && (
        <CommentForm onSubmit={handleReplySubmit} isReply={true} />
      )}

      {/* 재귀: 대댓글 렌더 */}
      {comment.replies && comment.replies.length > 0 && (
        <div className="ml-6 border-l border-gray-300 pl-4">
          {comment.replies.map(reply => (
            <CommentItem
              key={reply.id}
              comment={reply}
              onDelete={onDelete}
              onReplySubmit={onReplySubmit}
              showReplyForm={showReplyForm}
              onToggleReplyForm={onToggleReplyForm}
            />
          ))}
        </div>
      )}
    </div>
  );
}
```

### 재귀 함수 - CommentSection.tsx
```typescript
// 깊은 위치의 대댓글 추가
const addReplyRecursive = (
  arr: Comment[],
  parentId: number,
  reply: Comment,
): Comment[] => {
  return arr.map(c => ({
    ...c,
    replies: c.id === parentId
      ? [...c.replies, reply]
      : addReplyRecursive(c.replies, parentId, reply),
  }));
};

// 깊은 위치의 댓글 삭제
const deleteCommentRecursive = (arr: Comment[], id: number): Comment[] => {
  return arr
    .filter(c => c.id !== id)
    .map(c => ({
      ...c,
      replies: deleteCommentRecursive(c.replies, id),
    }));
};
```

---

## 🚀 다음 단계 (Phase 2)

### 우선순위 기능
1. **댓글 수정**
   - Edit 버튼 추가
   - 수정 폼 (CommentForm 재사용)
   - PATCH /api/v1/comments/{commentId}
   - "수정됨" 표시

2. **댓글 좋아요**
   - 하트 아이콘
   - 좋아요 카운트
   - POST/DELETE /api/v1/comments/{commentId}/likes

3. **댓글 신고**
   - 신고 버튼
   - 신고 모달
   - POST /api/v1/comments/{commentId}/reports

4. **무한 깊이 제한**
   - MAX_REPLY_DEPTH = 3 설정
   - 깊이 초과 시 답글 버튼 비활성화

### 성능 개선
1. **메모이제이션**
   - React.memo(CommentItem)
   - useMemo for calculateDepth

2. **페이지네이션**
   - 최상위 댓글만 페이징 (10개씩)
   - 대댓글은 전체 로드

3. **무한 스크롤**
   - Intersection Observer API
   - "더보기" 버튼 대안

---

## 📚 문서 목록

| 문서 | 설명 |
|------|------|
| 기획서.md | 기능 정의 및 요구사항 |
| 설계서.md | 구현 설계 및 아키텍처 |
| 구현완료.md | 이 파일 |
| QA보고서.md | 테스트 결과 |

---

## 🎓 학습 포인트

### 재귀 구조 패턴
```typescript
// React에서 재귀 구조는 자신을 렌더하는 컴포넌트로 구현
<CommentItem comment={item} ... />
  // item.replies를 다시 CommentItem으로 렌더
  {item.replies.map(r => <CommentItem comment={r} ... />)}
```

### 불변성 유지
```typescript
// ❌ 직접 수정
arr[0].replies.push(newItem);

// ✅ 새 배열 생성
const newArr = arr.map(item => ({
  ...item,
  replies: item.id === parentId 
    ? [...item.replies, newItem]
    : item.replies
}));
```

### 깊은 상태 관리
```typescript
// 객체 키로 각 댓글별 상태 추적
const [showReplyForm, setShowReplyForm] = useState<{
  [commentId: number]: boolean;
}>({});

// 토글
setShowReplyForm({
  ...showReplyForm,
  [commentId]: !showReplyForm[commentId]
});
```

---

## ✅ 배포 체크리스트

### Phase 1 완료 항목
- [x] 기획 문서 작성
- [x] 설계 문서 작성
- [x] 컴포넌트 구현
- [x] 타입 정의
- [x] 모킹 데이터
- [x] QA 테스트
- [x] 구현 문서 작성

### Phase 2 준비 항목
- [ ] 수정 기능 구현
- [ ] 좋아요 기능 구현
- [ ] 신고 기능 구현
- [ ] 깊이 제한 추가
- [ ] 성능 최적화
- [ ] E2E 테스트

### 배포 전 필수 사항
- [ ] 백엔드 API 완성 (대기 중)
- [ ] commentService 활성화
- [ ] API 테스트
- [ ] 성능 프로파일링
- [ ] 보안 검토

---

## 📞 문의

### 개발자
- **담당**: Claude AI
- **작업 기간**: 2026-04-20

### 관련 인물
- **기획**: 사용자
- **QA**: Claude AI
- **배포**: 예정 (백엔드 API 완성 후)

---

## 📋 최종 요약

✅ **댓글/대댓글 기능 Phase 1이 완료되었습니다.**

- 무한 깊이를 지원하는 재귀 구조
- 실시간 UI 업데이트
- 권한 기반 삭제
- 완전한 타입 안전성
- 포괄적인 QA 검증

**다음 단계**: 백엔드 API 준비 후 연동 및 Phase 2 기능 구현
